# Server remove — copy to devmin/scripts/docker/<repo_name>/ and adapt.
# Tear down this stack on the remote only: containers, volumes, image. No install.

stack_name: myapp
image_tag: myapp:latest
compose_file: ../../../docker-compose.yml
dockerfile: ../../../dockerfile
docker_network: myapp-net
internal_port: ""
publish_port: ""
delete_volume: "yes"
delete_image: "yes"
build_image_on: local
ssh: "<user>@<host>"
volume_dir: /opt/stacks/myapp
